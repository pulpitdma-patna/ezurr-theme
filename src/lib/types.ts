export type CatalogProduct = {
  img: string;
  brand: string;
  name: string;
  price: string;
  strike: string;
};

export type GameCardProduct = {
  title: string;
  tag: string;
  sub: string;
  bg: string;
  value: string;
  name: string;
  price: string;
};
